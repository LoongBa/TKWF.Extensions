# .NET
bin/
obj/
*.user
*.suo

# xCodeGen 生成物（.g.cs）——【入库】源码库自包含原则（2026-09-14 裁定）：
# 本仓库是源码库（消费方 ProjectReference/源码审查），生成物是提交的组成部分——
# fresh clone / CI 必须无前置步骤可构建；提交必须可复现。
# 重生成纪律：实体/模板变更后运行 pwsh .xCodeGen/run-xcodegen.ps1，将更新的 .g.cs 一并提交。
# obj\bin 下的分析器中间产物仍由上方 obj/、bin/ 目录规则完整忽略，不受影响。
# 对齐说明：DMP-Lite 模式"不入库"依赖构建期 _XCG_Run 自动生成；本仓库未接入该目标，
# 故不入库即产生"孤儿生成物"（fresh clone 不可构建、CI 需手动前置）——判定为错误政策，已废止。
*.userprefs
*.userosscache
*.sln.docstates
build/refs/

# Test results
TestResults/
[Tt]est[Rr]esult*/
*.trx
*.coverage
*.coveragexml
*.ncov
.ncprev
junit.xml
coverage.cobertura.xml

# IDE
.vs/
.vscode/
.idea/
*.swp
*.swo
.ionide/

# OS
.DS_Store
Thumbs.db

# User-specific
*.user
*.suo
*_i.c
*_p.c
*.nupkg
*.snupkg

# Secrets
.env
.env.local
appsettings*.local.json