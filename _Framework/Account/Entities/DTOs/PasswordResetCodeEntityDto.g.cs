// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: 13b76e26bda94be32d221c835c5fe8594747b269027404bb1471e718a8fdfdee]
// 模型: PasswordResetCodeEntity (TKWF.Ext.Account.PasswordResetCodeEntity)
// 生成时间: 2026-09-14 08:05:44
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.Account;

namespace TKWF.Ext.Account.DTOs;
/// <summary>条件工厂（V4.9）。支持两种使用方式：     <list type="bullet">     <item><c>PasswordResetCodeEntity.Conditions.ByXxx(value)</c> — 直接返回表达式（已有代码兼容）</item>     <item><c>PasswordResetCodeEntity.Where.ByXxx(value).ByYyy(value)</c> — 链式组合，隐式转换到 Expression</item>     </list></summary>
public partial record PasswordResetCodeEntityDto : IDomainDto<PasswordResetCodeEntity>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键（自增）。 </summary>
    public long Id { get; init; }
    /// <summary> 用户名。 </summary>
    public required string UserName { get; init; }
    /// <summary> 重置码（随机生成）。 </summary>
    public required string ResetCode { get; init; }
    /// <summary> 过期时间。     <para>注：FreeSql SQLite 对 DateTimeOffset 写入不可靠（读回丢失），统一用 DateTime（本地时间语义）。</para> </summary>
    public DateTime ExpireTime { get; init; }
    /// <summary> 是否已使用（幂等消费）。 </summary>
    public bool IsUsed { get; init; }
    /// <summary> 创建时间。 </summary>
    public required DateTimeOffset CreateTime { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static PasswordResetCodeEntityDto FromEntity(PasswordResetCodeEntity entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new PasswordResetCodeEntityDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            UserName = entity.UserName,
            ResetCode = entity.ResetCode,
            ExpireTime = entity.ExpireTime,
            IsUsed = entity.IsUsed,
            CreateTime = entity.CreateTime,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<PasswordResetCodeEntity, PasswordResetCodeEntityDto>> SelectExpression => e => new PasswordResetCodeEntityDto
    {
        Id = e.Id,
        UserName = e.UserName,
        ResetCode = e.ResetCode,
        ExpireTime = e.ExpireTime,
        IsUsed = e.IsUsed,
        CreateTime = e.CreateTime,
    };
    #endregion

    PasswordResetCodeEntity IDomainDto<PasswordResetCodeEntity>.ApplyToEntity(PasswordResetCodeEntity entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(UserName), d => d.UserName, (e, v) => e.UserName = v )
            .Map(nameof(ResetCode), d => d.ResetCode, (e, v) => e.ResetCode = v )
            .Map(nameof(ExpireTime), d => d.ExpireTime, (e, v) => e.ExpireTime = v )
            .Map(nameof(IsUsed), d => d.IsUsed, (e, v) => e.IsUsed = v )
            .Map(nameof(CreateTime), d => d.CreateTime, (e, v) => e.CreateTime = v )
        ;
        return entity;
    }

    PasswordResetCodeEntity IDomainDto<PasswordResetCodeEntity>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(PasswordResetCodeEntityDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<PasswordResetCodeEntity>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<PasswordResetCodeEntity, PasswordResetCodeEntityDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class PasswordResetCodeEntityDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static PasswordResetCodeEntityDto ToDto(this PasswordResetCodeEntity entity) => PasswordResetCodeEntityDto.FromEntity(entity);
}

