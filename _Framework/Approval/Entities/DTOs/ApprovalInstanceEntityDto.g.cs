// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: 33a921f638e5540fd080c2321fe69b9172f7f9ba88fe25bafd52429567d0e1d2]
// 模型: ApprovalInstanceEntity (TKWF.Ext.Approval.ApprovalInstanceEntity)
// 生成时间: 2026-09-14 09:20:39
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.Approval;

namespace TKWF.Ext.Approval.DTOs;
/// <summary>审批实例实体——审批流程的业务实例（业务实体弱关联 + 状态机 + 防重复唯一约束）。     <para>唯一约束 <c>UX_ApprovalInstance_Active</c>（BusinessType+BusinessId+IsActive）——活动实例（IsActive=true）同业务唯一防重复；     终态 IsActive=false 释放约束，允许重新提交新实例（评审 C2）。</para></summary>
public partial record ApprovalInstanceEntityDto : IDomainDto<ApprovalInstanceEntity>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键（自增）。 </summary>
    public long Id { get; init; }
    /// <summary> 关联审批流定义 ID。 </summary>
    public long FlowId { get; init; }
    /// <summary> 关联审批流编码（快照——冗余查询便利）。 </summary>
    public required string FlowCode { get; init; }
    /// <summary> 业务实体类型名（如 "Expense"、"LeaveRequest"）。 </summary>
    public required string BusinessType { get; init; }
    /// <summary> 业务实体 ID（如 "exp-001"、"lr-002"）。 </summary>
    public required string BusinessId { get; init; }
    /// <summary> 实例状态（<see cref="T:TKWF.Ext.Approval.ApprovalInstanceStatus"/>）。 </summary>
    public required ApprovalInstanceStatus Status { get; init; }
    /// <summary> 活动标志（C2 核心）：true=活动实例（Draft/Pending），false=终态（Approved/Rejected/Withdrawn）。     <para>UX_ApprovalInstance_Active 唯一约束保证同业务仅一个活动实例；终态释放约束允许重新提交。</para> </summary>
    public bool IsActive { get; init; }
    /// <summary> 当前步骤序号（从 0 开始）。 </summary>
    public int CurrentStepIndex { get; init; }
    /// <summary> 提交人（用户 ID 快照）。 </summary>
    public required string Submitter { get; init; }
    /// <summary> 提交时间（SubmitAsync 写入）。 </summary>
    public DateTime? SubmittedAt { get; init; }
    /// <summary> 通过时间（终态 Approved 写入）。 </summary>
    public DateTime? ApprovedAt { get; init; }
    /// <summary> 驳回时间（终态 Rejected 写入）。 </summary>
    public DateTime? RejectedAt { get; init; }
    /// <summary> 驳回/撤回原因（可选）。 </summary>
    public string? Reason { get; init; }
    /// <summary> 业务数据快照 JSON（可选——轻量快照，不跨模块引用实体）。 </summary>
    public string? BusinessDataJson { get; init; }
    /// <summary> 关联 ID（可选——用于跨系统关联追踪）。 </summary>
    public string? CorrelationId { get; init; }
    /// <summary> 创建时间（UTC）。 </summary>
    public DateTime CreateTime { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static ApprovalInstanceEntityDto FromEntity(ApprovalInstanceEntity entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new ApprovalInstanceEntityDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            FlowId = entity.FlowId,
            FlowCode = entity.FlowCode,
            BusinessType = entity.BusinessType,
            BusinessId = entity.BusinessId,
            Status = entity.Status,
            IsActive = entity.IsActive,
            CurrentStepIndex = entity.CurrentStepIndex,
            Submitter = entity.Submitter,
            SubmittedAt = entity.SubmittedAt,
            ApprovedAt = entity.ApprovedAt,
            RejectedAt = entity.RejectedAt,
            Reason = entity.Reason,
            BusinessDataJson = entity.BusinessDataJson,
            CorrelationId = entity.CorrelationId,
            CreateTime = entity.CreateTime,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<ApprovalInstanceEntity, ApprovalInstanceEntityDto>> SelectExpression => e => new ApprovalInstanceEntityDto
    {
        Id = e.Id,
        FlowId = e.FlowId,
        FlowCode = e.FlowCode,
        BusinessType = e.BusinessType,
        BusinessId = e.BusinessId,
        Status = e.Status,
        IsActive = e.IsActive,
        CurrentStepIndex = e.CurrentStepIndex,
        Submitter = e.Submitter,
        SubmittedAt = e.SubmittedAt,
        ApprovedAt = e.ApprovedAt,
        RejectedAt = e.RejectedAt,
        Reason = e.Reason,
        BusinessDataJson = e.BusinessDataJson,
        CorrelationId = e.CorrelationId,
        CreateTime = e.CreateTime,
    };
    #endregion

    ApprovalInstanceEntity IDomainDto<ApprovalInstanceEntity>.ApplyToEntity(ApprovalInstanceEntity entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(FlowId), d => d.FlowId, (e, v) => e.FlowId = v )
            .Map(nameof(FlowCode), d => d.FlowCode, (e, v) => e.FlowCode = v )
            .Map(nameof(BusinessType), d => d.BusinessType, (e, v) => e.BusinessType = v )
            .Map(nameof(BusinessId), d => d.BusinessId, (e, v) => e.BusinessId = v )
            .Map(nameof(Status), d => d.Status, (e, v) => e.Status = v )
            .Map(nameof(IsActive), d => d.IsActive, (e, v) => e.IsActive = v )
            .Map(nameof(CurrentStepIndex), d => d.CurrentStepIndex, (e, v) => e.CurrentStepIndex = v )
            .Map(nameof(Submitter), d => d.Submitter, (e, v) => e.Submitter = v )
            .Map(nameof(SubmittedAt), d => d.SubmittedAt, (e, v) => e.SubmittedAt = v )
            .Map(nameof(ApprovedAt), d => d.ApprovedAt, (e, v) => e.ApprovedAt = v )
            .Map(nameof(RejectedAt), d => d.RejectedAt, (e, v) => e.RejectedAt = v )
            .Map(nameof(Reason), d => d.Reason, (e, v) => e.Reason = v )
            .Map(nameof(BusinessDataJson), d => d.BusinessDataJson, (e, v) => e.BusinessDataJson = v )
            .Map(nameof(CorrelationId), d => d.CorrelationId, (e, v) => e.CorrelationId = v )
            .Map(nameof(CreateTime), d => d.CreateTime, (e, v) => e.CreateTime = v )
        ;
        return entity;
    }

    ApprovalInstanceEntity IDomainDto<ApprovalInstanceEntity>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(ApprovalInstanceEntityDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<ApprovalInstanceEntity>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<ApprovalInstanceEntity, ApprovalInstanceEntityDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class ApprovalInstanceEntityDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static ApprovalInstanceEntityDto ToDto(this ApprovalInstanceEntity entity) => ApprovalInstanceEntityDto.FromEntity(entity);
}

