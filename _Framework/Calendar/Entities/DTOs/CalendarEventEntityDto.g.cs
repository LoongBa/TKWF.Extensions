// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: a0d33e331dd042dacda88c207d4d05aa617a80cf2173c97bcd190cbbcc0d9b03]
// 模型: CalendarEventEntity (TKWF.Ext.Calendar.CalendarEventEntity)
// 生成时间: 2026-09-14 08:05:51
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.Calendar;

namespace TKWF.Ext.Calendar.DTOs;
/// <summary>日历事件实体——单次或重复事件（归属日历 + UTC 时间 + 全天标记，F2/F3）。     <para>SG1 化：声明式实体——<c>partial</c> + <c>[DomainGenerateCode]</c>；     保留 BCL <c>[Table("CalendarEvent")]</c>；列映射用 FreeSql <c>[Column]</c>（全限定）。</para>     <para>重复语义（ADR-Calendar C1/C2/P4）：<c>RecurrenceRule</c> 存 <c>RecurrenceRule.ToString()</c> 规范规则串     （可空 = 单次事件；列名存规则文本非 JSON）；<c>RecurrenceEndUtc</c> 为冗余截止（UNTIL 直取 / COUNT 与展开算法     <b>同源</b>推导取最后 occurrence——写入时受上限保护运行展开，偏差构造性消除），加速重复路径范围预筛。</para>     <para>时长语义（C1/P1/P2）：<c>EndUtc</c> 可空（空 = 瞬时事件）；重复事件允许非空 = <b>首 occurrence 时长锚点</b>     （occurrence 时长 = EndUtc - StartUtc，展开时起点 + 偏移）；AllDay 事件 EndUtc 落库 = 次日 00:00 UTC。</para>     <para>删除语义：物理删除（不声明 IsDeleted，hasSoftDelete:false）；occurrence 不物化——展开是查询时计算。</para>     <para>索引（P8）：<c>IX_CalendarEvent_CalendarId_StartUtc</c>（单次/默认路径）+ <c>IX_CalendarEvent_CalendarId_RecurrenceEndUtc</c>（重复路径预筛）。</para></summary>
public partial record CalendarEventEntityDto : IDomainDto<CalendarEventEntity>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键（自增）。 </summary>
    public long Id { get; init; }
    /// <summary> 归属日历 Id（引用守卫：创建/变更时校验日历存在）。 </summary>
    public long CalendarId { get; init; }
    /// <summary> 事件标题。 </summary>
    public required string Title { get; init; }
    /// <summary> 描述。 </summary>
    public string? Description { get; init; }
    /// <summary> 地点。 </summary>
    public string? Location { get; init; }
    /// <summary> 开始时间（UTC，主时间）。 </summary>
    public DateTime StartUtc { get; init; }
    /// <summary> 结束时间（UTC，可空 = 瞬时；重复事件 = 首 occurrence 时长锚点）。 </summary>
    public DateTime? EndUtc { get; init; }
    /// <summary> 全天标记（UTC 日期语义；EndUtc 落库 = 次日 00:00，P2）。 </summary>
    public bool AllDay { get; init; }
    /// <summary> 重复规则规范串（RecurrenceRule.ToString()；可空 = 单次事件）。 </summary>
    public string? RecurrenceRule { get; init; }
    /// <summary> 冗余截止（UNTIL 直取 / COUNT 同源推导 / 无界 null；重复路径范围预筛，C2/P8）。 </summary>
    public DateTime? RecurrenceEndUtc { get; init; }
    /// <summary> 创建时间（UTC，显式声明）。 </summary>
    public DateTime CreateTime { get; init; }
    /// <summary> 更新时间（UTC，显式声明）。 </summary>
    public DateTime UpdateTime { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static CalendarEventEntityDto FromEntity(CalendarEventEntity entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new CalendarEventEntityDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            CalendarId = entity.CalendarId,
            Title = entity.Title,
            Description = entity.Description,
            Location = entity.Location,
            StartUtc = entity.StartUtc,
            EndUtc = entity.EndUtc,
            AllDay = entity.AllDay,
            RecurrenceRule = entity.RecurrenceRule,
            RecurrenceEndUtc = entity.RecurrenceEndUtc,
            CreateTime = entity.CreateTime,
            UpdateTime = entity.UpdateTime,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<CalendarEventEntity, CalendarEventEntityDto>> SelectExpression => e => new CalendarEventEntityDto
    {
        Id = e.Id,
        CalendarId = e.CalendarId,
        Title = e.Title,
        Description = e.Description,
        Location = e.Location,
        StartUtc = e.StartUtc,
        EndUtc = e.EndUtc,
        AllDay = e.AllDay,
        RecurrenceRule = e.RecurrenceRule,
        RecurrenceEndUtc = e.RecurrenceEndUtc,
        CreateTime = e.CreateTime,
        UpdateTime = e.UpdateTime,
    };
    #endregion

    CalendarEventEntity IDomainDto<CalendarEventEntity>.ApplyToEntity(CalendarEventEntity entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(CalendarId), d => d.CalendarId, (e, v) => e.CalendarId = v )
            .Map(nameof(Title), d => d.Title, (e, v) => e.Title = v )
            .Map(nameof(Description), d => d.Description, (e, v) => e.Description = v )
            .Map(nameof(Location), d => d.Location, (e, v) => e.Location = v )
            .Map(nameof(StartUtc), d => d.StartUtc, (e, v) => e.StartUtc = v )
            .Map(nameof(EndUtc), d => d.EndUtc, (e, v) => e.EndUtc = v )
            .Map(nameof(AllDay), d => d.AllDay, (e, v) => e.AllDay = v )
            .Map(nameof(RecurrenceRule), d => d.RecurrenceRule, (e, v) => e.RecurrenceRule = v )
            .Map(nameof(RecurrenceEndUtc), d => d.RecurrenceEndUtc, (e, v) => e.RecurrenceEndUtc = v )
            .Map(nameof(CreateTime), d => d.CreateTime, (e, v) => e.CreateTime = v )
            .Map(nameof(UpdateTime), d => d.UpdateTime, (e, v) => e.UpdateTime = v )
        ;
        return entity;
    }

    CalendarEventEntity IDomainDto<CalendarEventEntity>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(CalendarEventEntityDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<CalendarEventEntity>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<CalendarEventEntity, CalendarEventEntityDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class CalendarEventEntityDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static CalendarEventEntityDto ToDto(this CalendarEventEntity entity) => CalendarEventEntityDto.FromEntity(entity);
}

