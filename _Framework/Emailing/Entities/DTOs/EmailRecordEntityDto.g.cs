// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: 0d22343cf43cc60a9ff174465631105fbc685629588e8a0d1b4cddcd8beae888]
// 模型: EmailRecordEntity (TKWF.Ext.Emailing.EmailRecordEntity)
// 生成时间: 2026-09-14 08:05:56
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.Emailing;

namespace TKWF.Ext.Emailing.DTOs;
/// <summary>邮件记录表实体——存储发送邮件的记录（收件人、发件人、主题、正文、状态、错误信息等）。     <para>SG1 化：声明式实体——<c>partial</c> + <c>[DomainGenerateCode]</c>。     SG1 自动生成 IDomainEntity 部分与 DTO/DataService。</para>     <para>保留 BCL <c>[Table("EmailRecord")]</c>（<c>FreeSqlTableStructureSynchronizer</c> 靠它发现实体建表）；     列映射用 FreeSql <c>[Column]</c>（IsPrimary/IsIdentity/Position，全限定避免与 BCL Schema 特性名冲突）。</para></summary>
public partial record EmailRecordEntityDto : IDomainDto<EmailRecordEntity>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键（自增）。 </summary>
    public long Id { get; init; }
    /// <summary> 收件人（多个以逗号分隔）。 </summary>
    public required string To { get; init; }
    /// <summary> 发件人地址。 </summary>
    public string? From { get; init; }
    /// <summary> 邮件主题。 </summary>
    public required string Subject { get; init; }
    /// <summary> 邮件正文。 </summary>
    public string? Body { get; init; }
    /// <summary> 是否为 HTML 格式正文。 </summary>
    public bool IsHtml { get; init; }
    /// <summary> 发送状态（Pending / Sent / Failed）。 </summary>
    public required string Status { get; init; }
    /// <summary> 错误信息（发送失败时记录）。 </summary>
    public string? ErrorMessage { get; init; }
    /// <summary> 重试次数。 </summary>
    public int RetryCount { get; init; }
    /// <summary> 记录创建时间。 </summary>
    public DateTime CreateTime { get; init; }
    /// <summary> 实际发送时间（发送成功时记录）。 </summary>
    public DateTime? SendTime { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static EmailRecordEntityDto FromEntity(EmailRecordEntity entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new EmailRecordEntityDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            To = entity.To,
            From = entity.From,
            Subject = entity.Subject,
            Body = entity.Body,
            IsHtml = entity.IsHtml,
            Status = entity.Status,
            ErrorMessage = entity.ErrorMessage,
            RetryCount = entity.RetryCount,
            CreateTime = entity.CreateTime,
            SendTime = entity.SendTime,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<EmailRecordEntity, EmailRecordEntityDto>> SelectExpression => e => new EmailRecordEntityDto
    {
        Id = e.Id,
        To = e.To,
        From = e.From,
        Subject = e.Subject,
        Body = e.Body,
        IsHtml = e.IsHtml,
        Status = e.Status,
        ErrorMessage = e.ErrorMessage,
        RetryCount = e.RetryCount,
        CreateTime = e.CreateTime,
        SendTime = e.SendTime,
    };
    #endregion

    EmailRecordEntity IDomainDto<EmailRecordEntity>.ApplyToEntity(EmailRecordEntity entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(To), d => d.To, (e, v) => e.To = v )
            .Map(nameof(From), d => d.From, (e, v) => e.From = v )
            .Map(nameof(Subject), d => d.Subject, (e, v) => e.Subject = v )
            .Map(nameof(Body), d => d.Body, (e, v) => e.Body = v )
            .Map(nameof(IsHtml), d => d.IsHtml, (e, v) => e.IsHtml = v )
            .Map(nameof(Status), d => d.Status, (e, v) => e.Status = v )
            .Map(nameof(ErrorMessage), d => d.ErrorMessage, (e, v) => e.ErrorMessage = v )
            .Map(nameof(RetryCount), d => d.RetryCount, (e, v) => e.RetryCount = v )
            .Map(nameof(CreateTime), d => d.CreateTime, (e, v) => e.CreateTime = v )
            .Map(nameof(SendTime), d => d.SendTime, (e, v) => e.SendTime = v )
        ;
        return entity;
    }

    EmailRecordEntity IDomainDto<EmailRecordEntity>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(EmailRecordEntityDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<EmailRecordEntity>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<EmailRecordEntity, EmailRecordEntityDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class EmailRecordEntityDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static EmailRecordEntityDto ToDto(this EmailRecordEntity entity) => EmailRecordEntityDto.FromEntity(entity);
}

