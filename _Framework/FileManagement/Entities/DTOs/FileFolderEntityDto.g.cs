// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: db797fd6fb4a82add45f3ecce719efaca265b0bd24548bb96befbbf7fb3118c7]
// 模型: FileFolderEntity (TKWF.Ext.FileManagement.FileFolderEntity)
// 生成时间: 2026-09-09 11:21:48
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.FileManagement;

namespace TKWF.Ext.FileManagement.DTOs;
/// <summary>条件工厂（V4.9）。支持两种使用方式：     <list type="bullet">     <item><c>FileFolderEntity.Conditions.ByXxx(value)</c> — 直接返回表达式（已有代码兼容）</item>     <item><c>FileFolderEntity.Where.ByXxx(value).ByYyy(value)</c> — 链式组合，隐式转换到 Expression</item>     </list></summary>
public partial record FileFolderEntityDto : IDomainDto<FileFolderEntity>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键（自增）。 </summary>
    public long Id { get; init; }
    /// <summary> 目录编码（库级唯一；构建 Path 段，创建后不可改——C3）。 </summary>
    public required string Code { get; init; }
    /// <summary> 目录名称（可含任意字符，不进 Path；重命名只改本字段）。 </summary>
    public required string Name { get; init; }
    /// <summary> 父目录 Id（自引用；null = 根目录）。 </summary>
    public long? ParentId { get; init; }
    /// <summary> 层级（根 = 0；子 = 父 + 1）。 </summary>
    public int Level { get; init; }
    /// <summary> 物化路径（"/Code/"；子 = 父 Path + Code + "/"；≤1024 守卫，对齐 OU C3）。 </summary>
    public required string Path { get; init; }
    /// <summary> 同级排序（小值在前；默认同级末尾 max+1）。 </summary>
    public int SortOrder { get; init; }
    /// <summary> 创建时间（UTC，显式声明）。 </summary>
    public DateTime CreateTime { get; init; }
    /// <summary> 更新时间（UTC，显式声明）。 </summary>
    public DateTime UpdateTime { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static FileFolderEntityDto FromEntity(FileFolderEntity entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new FileFolderEntityDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            Code = entity.Code,
            Name = entity.Name,
            ParentId = entity.ParentId,
            Level = entity.Level,
            Path = entity.Path,
            SortOrder = entity.SortOrder,
            CreateTime = entity.CreateTime,
            UpdateTime = entity.UpdateTime,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<FileFolderEntity, FileFolderEntityDto>> SelectExpression => e => new FileFolderEntityDto
    {
        Id = e.Id,
        Code = e.Code,
        Name = e.Name,
        ParentId = e.ParentId,
        Level = e.Level,
        Path = e.Path,
        SortOrder = e.SortOrder,
        CreateTime = e.CreateTime,
        UpdateTime = e.UpdateTime,
    };
    #endregion

    FileFolderEntity IDomainDto<FileFolderEntity>.ApplyToEntity(FileFolderEntity entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(Code), d => d.Code, (e, v) => e.Code = v )
            .Map(nameof(Name), d => d.Name, (e, v) => e.Name = v )
            .Map(nameof(ParentId), d => d.ParentId, (e, v) => e.ParentId = v )
            .Map(nameof(Level), d => d.Level, (e, v) => e.Level = v )
            .Map(nameof(Path), d => d.Path, (e, v) => e.Path = v )
            .Map(nameof(SortOrder), d => d.SortOrder, (e, v) => e.SortOrder = v )
            .Map(nameof(CreateTime), d => d.CreateTime, (e, v) => e.CreateTime = v )
            .Map(nameof(UpdateTime), d => d.UpdateTime, (e, v) => e.UpdateTime = v )
        ;
        return entity;
    }

    FileFolderEntity IDomainDto<FileFolderEntity>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(FileFolderEntityDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<FileFolderEntity>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<FileFolderEntity, FileFolderEntityDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class FileFolderEntityDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static FileFolderEntityDto ToDto(this FileFolderEntity entity) => FileFolderEntityDto.FromEntity(entity);
}

