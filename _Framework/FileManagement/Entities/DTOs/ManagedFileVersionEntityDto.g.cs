// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: f8c39915085975339617191c547f8dbaaa73baeaef024afc7698be96a1123a88]
// 模型: ManagedFileVersionEntity (TKWF.Ext.FileManagement.ManagedFileVersionEntity)
// 生成时间: 2026-09-14 08:05:59
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.FileManagement;

namespace TKWF.Ext.FileManagement.DTOs;
/// <summary>条件工厂（V4.9）。支持两种使用方式：     <list type="bullet">     <item><c>ManagedFileVersionEntity.Conditions.ByXxx(value)</c> — 直接返回表达式（已有代码兼容）</item>     <item><c>ManagedFileVersionEntity.Where.ByXxx(value).ByYyy(value)</c> — 链式组合，隐式转换到 Expression</item>     </list></summary>
public partial record ManagedFileVersionEntityDto : IDomainDto<ManagedFileVersionEntity>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键（自增）。 </summary>
    public long Id { get; init; }
    /// <summary> 所属文件（对应 <see cref="P:TKWF.Ext.FileManagement.ManagedFileEntity.Id"/>；IX 索引 + FileId+Version 联合唯一）。 </summary>
    public long FileId { get; init; }
    /// <summary> 版本号（1 起递增——首版 1，后续 max+1；与 FileId 联合唯一，并发兜底）。 </summary>
    public int Version { get; init; }
    /// <summary> 该版本 Blob 存储相对路径（形如 {guid}/{name}——每次上传新路径，旧版本 Blob 天然保留）。 </summary>
    public required string StoredPath { get; init; }
    /// <summary> 该版本扩展名（小写含点；对齐主表——回滚不回填主表 Extension，P2-3）。 </summary>
    public required string Extension { get; init; }
    /// <summary> 该版本 MIME ContentType（服务端推导）。 </summary>
    public string? ContentType { get; init; }
    /// <summary> 该版本文件大小（字节）。 </summary>
    public long Size { get; init; }
    /// <summary> 该版本文件内容 SHA256（十六进制小写）。 </summary>
    public required string Sha256 { get; init; }
    /// <summary> 该版本上传者名称（审计字段）。 </summary>
    public string? UploaderName { get; init; }
    /// <summary> 版本创建时间（UTC；CanUpdate=false——版本行 append-only 不可变，P2-7）。 </summary>
    public DateTime CreateTime { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static ManagedFileVersionEntityDto FromEntity(ManagedFileVersionEntity entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new ManagedFileVersionEntityDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            FileId = entity.FileId,
            Version = entity.Version,
            StoredPath = entity.StoredPath,
            Extension = entity.Extension,
            ContentType = entity.ContentType,
            Size = entity.Size,
            Sha256 = entity.Sha256,
            UploaderName = entity.UploaderName,
            CreateTime = entity.CreateTime,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<ManagedFileVersionEntity, ManagedFileVersionEntityDto>> SelectExpression => e => new ManagedFileVersionEntityDto
    {
        Id = e.Id,
        FileId = e.FileId,
        Version = e.Version,
        StoredPath = e.StoredPath,
        Extension = e.Extension,
        ContentType = e.ContentType,
        Size = e.Size,
        Sha256 = e.Sha256,
        UploaderName = e.UploaderName,
        CreateTime = e.CreateTime,
    };
    #endregion

    ManagedFileVersionEntity IDomainDto<ManagedFileVersionEntity>.ApplyToEntity(ManagedFileVersionEntity entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(FileId), d => d.FileId, (e, v) => e.FileId = v )
            .Map(nameof(Version), d => d.Version, (e, v) => e.Version = v )
            .Map(nameof(StoredPath), d => d.StoredPath, (e, v) => e.StoredPath = v )
            .Map(nameof(Extension), d => d.Extension, (e, v) => e.Extension = v )
            .Map(nameof(ContentType), d => d.ContentType, (e, v) => e.ContentType = v )
            .Map(nameof(Size), d => d.Size, (e, v) => e.Size = v )
            .Map(nameof(Sha256), d => d.Sha256, (e, v) => e.Sha256 = v )
            .Map(nameof(UploaderName), d => d.UploaderName, (e, v) => e.UploaderName = v )
            .Map(nameof(CreateTime), d => d.CreateTime, (e, v) => e.CreateTime = v )
        ;
        return entity;
    }

    ManagedFileVersionEntity IDomainDto<ManagedFileVersionEntity>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(ManagedFileVersionEntityDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<ManagedFileVersionEntity>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<ManagedFileVersionEntity, ManagedFileVersionEntityDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class ManagedFileVersionEntityDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static ManagedFileVersionEntityDto ToDto(this ManagedFileVersionEntity entity) => ManagedFileVersionEntityDto.FromEntity(entity);
}

