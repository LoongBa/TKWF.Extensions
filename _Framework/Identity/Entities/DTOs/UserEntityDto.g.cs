// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: 98efd7c55c231f127282a99087b2359580a273163ded74c6971294b9876f1617]
// 模型: UserEntity (TKWF.Ext.Identity.UserEntity)
// 生成时间: 2026-09-14 08:06:01
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.Identity;

namespace TKWF.Ext.Identity.DTOs;
/// <summary>条件工厂（V4.9）。支持两种使用方式：     <list type="bullet">     <item><c>UserEntity.Conditions.ByXxx(value)</c> — 直接返回表达式（已有代码兼容）</item>     <item><c>UserEntity.Where.ByXxx(value).ByYyy(value)</c> — 链式组合，隐式转换到 Expression</item>     </list></summary>
public partial record UserEntityDto : IDomainDto<UserEntity>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键（自增）。 </summary>
    public long Id { get; init; }
    /// <summary> 登录名（唯一，不区分大小写存储为规范化值）。 </summary>
    public required string UserName { get; init; }
    /// <summary> 规范化用户名（大写，用于大小写无关查询）。 </summary>
    public required string NormalizedUserName { get; init; }
    /// <summary> 显示名。 </summary>
    public required string DisplayName { get; init; }
    /// <summary> 密码散列（PasswordHasher 输出，格式 "Iterations.Salt.Hash"）。 </summary>
    public string? PasswordHash { get; init; }
    /// <summary> 邮箱。 </summary>
    public string? Email { get; init; }
    /// <summary> 手机号。 </summary>
    public string? Phone { get; init; }
    /// <summary> 是否启用（默认 true；禁用用户无法登录）。 </summary>
    public bool IsActive { get; init; }
    /// <summary> 创建时间。 </summary>
    public required DateTimeOffset CreateTime { get; init; }
    /// <summary> 更新时间。 </summary>
    public required DateTimeOffset UpdateTime { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static UserEntityDto FromEntity(UserEntity entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new UserEntityDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            UserName = entity.UserName,
            NormalizedUserName = entity.NormalizedUserName,
            DisplayName = entity.DisplayName,
            PasswordHash = entity.PasswordHash,
            Email = entity.Email,
            Phone = entity.Phone,
            IsActive = entity.IsActive,
            CreateTime = entity.CreateTime,
            UpdateTime = entity.UpdateTime,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<UserEntity, UserEntityDto>> SelectExpression => e => new UserEntityDto
    {
        Id = e.Id,
        UserName = e.UserName,
        NormalizedUserName = e.NormalizedUserName,
        DisplayName = e.DisplayName,
        PasswordHash = e.PasswordHash,
        Email = e.Email,
        Phone = e.Phone,
        IsActive = e.IsActive,
        CreateTime = e.CreateTime,
        UpdateTime = e.UpdateTime,
    };
    #endregion

    UserEntity IDomainDto<UserEntity>.ApplyToEntity(UserEntity entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(UserName), d => d.UserName, (e, v) => e.UserName = v )
            .Map(nameof(NormalizedUserName), d => d.NormalizedUserName, (e, v) => e.NormalizedUserName = v )
            .Map(nameof(DisplayName), d => d.DisplayName, (e, v) => e.DisplayName = v )
            .Map(nameof(PasswordHash), d => d.PasswordHash, (e, v) => e.PasswordHash = v )
            .Map(nameof(Email), d => d.Email, (e, v) => e.Email = v )
            .Map(nameof(Phone), d => d.Phone, (e, v) => e.Phone = v )
            .Map(nameof(IsActive), d => d.IsActive, (e, v) => e.IsActive = v )
            .Map(nameof(CreateTime), d => d.CreateTime, (e, v) => e.CreateTime = v )
            .Map(nameof(UpdateTime), d => d.UpdateTime, (e, v) => e.UpdateTime = v )
        ;
        return entity;
    }

    UserEntity IDomainDto<UserEntity>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(UserEntityDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<UserEntity>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<UserEntity, UserEntityDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class UserEntityDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static UserEntityDto ToDto(this UserEntity entity) => UserEntityDto.FromEntity(entity);
}

