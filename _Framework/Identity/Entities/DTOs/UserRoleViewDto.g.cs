// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: 69301ff237895cb26d6810f283f898309c17c760e0b9020c05824bd8bd5fd184]
// 模型: UserRoleView (TKWF.Ext.Identity.UserRoleView)
// 生成时间: 2026-09-14 08:06:01
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.Identity;

namespace TKWF.Ext.Identity.DTOs;
/// <summary>用户-角色视图实体（VEntity）——跨表 JOIN <c>IdentityUserRole</c> → <c>IdentityRole</c>，按 UserId 单查询返回角色。     <para>V0.2.0：替代两步查询（先查 RoleId 集合再查 Role）——JOIN 下推 DB，单查询完成。     VEntity 只读：<c>IDomainViewEntity</c> 由 SG1 自动生成，禁 IEntityDAC 写操作。</para></summary>
public partial record UserRoleViewDto : IDomainDto<UserRoleView>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键——透传 Role.Id（每个 UserId 最多一条，唯一稳定）。 </summary>
    public long Id { get; init; }
    /// <summary> 用户 ID（外层过滤键，视图外参数化）。 </summary>
    public long UserId { get; init; }
    /// <summary> 角色名（如 "Admin"）。 </summary>
    public required string Name { get; init; }
    /// <summary> 角色显示名（如 "管理员"）。 </summary>
    public required string DisplayName { get; init; }
    /// <summary> 是否系统内置角色。 </summary>
    public bool IsSystemRole { get; init; }
    /// <summary> 角色创建时间（透传 Role.CreateTime，DateTimeOffset）。 </summary>
    public required DateTimeOffset CreateTime { get; init; }
    /// <summary> 角色更新时间（透传 Role.UpdateTime，DateTimeOffset）。 </summary>
    public required DateTimeOffset UpdateTime { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static UserRoleViewDto FromEntity(UserRoleView entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new UserRoleViewDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            UserId = entity.UserId,
            Name = entity.Name,
            DisplayName = entity.DisplayName,
            IsSystemRole = entity.IsSystemRole,
            CreateTime = entity.CreateTime,
            UpdateTime = entity.UpdateTime,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<UserRoleView, UserRoleViewDto>> SelectExpression => e => new UserRoleViewDto
    {
        Id = e.Id,
        UserId = e.UserId,
        Name = e.Name,
        DisplayName = e.DisplayName,
        IsSystemRole = e.IsSystemRole,
        CreateTime = e.CreateTime,
        UpdateTime = e.UpdateTime,
    };
    #endregion

    UserRoleView IDomainDto<UserRoleView>.ApplyToEntity(UserRoleView entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(UserId), d => d.UserId, (e, v) => e.UserId = v )
            .Map(nameof(Name), d => d.Name, (e, v) => e.Name = v )
            .Map(nameof(DisplayName), d => d.DisplayName, (e, v) => e.DisplayName = v )
            .Map(nameof(IsSystemRole), d => d.IsSystemRole, (e, v) => e.IsSystemRole = v )
            .Map(nameof(CreateTime), d => d.CreateTime, (e, v) => e.CreateTime = v )
            .Map(nameof(UpdateTime), d => d.UpdateTime, (e, v) => e.UpdateTime = v )
        ;
        return entity;
    }

    UserRoleView IDomainDto<UserRoleView>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(UserRoleViewDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<UserRoleView>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<UserRoleView, UserRoleViewDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class UserRoleViewDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static UserRoleViewDto ToDto(this UserRoleView entity) => UserRoleViewDto.FromEntity(entity);
}

