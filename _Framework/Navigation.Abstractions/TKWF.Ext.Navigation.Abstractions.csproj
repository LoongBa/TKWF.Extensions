<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup Label="Globals">
    <SccProjectName>SAK</SccProjectName>
    <SccProvider>SAK</SccProvider>
    <SccAuxPath>SAK</SccAuxPath>
    <SccLocalPath>SAK</SccLocalPath>
    <Platforms>AnyCPU;x64</Platforms>
  </PropertyGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <RootNamespace>TKWF.Ext.Navigation</RootNamespace>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <IsPackable>true</IsPackable>
    <MinVerTagPrefix>Navigation.Abstractions/v</MinVerTagPrefix>
    <PackageId>TKWF.Ext.Navigation.Abstractions</PackageId>
    <Description>TKWF 扩展抽象：导航契约（IMenuContributor/MenuItemDefinition/MenuConfigurationContext）——依赖倒置（ADR48 D7）；A+ 贡献者机制阶段 2 契约归位</Description>
  </PropertyGroup>

  <ItemGroup>
    <!-- 依赖矩阵（Oracle 条件 3）：MenuItemDefinition 用 PermissionLogic（Permissions.Abstractions）；
         3 契约零 CodeGeneration 引用（实测：IMenuContributor 无 using / MenuItemDefinition 仅 Permissions /
         MenuConfigurationContext 仅 BCL）——对齐 Account.Abstractions 纯契约精神（多一 Permissions 依赖） -->
    <ProjectReference Include="..\Permissions.Abstractions\TKWF.Ext.Permissions.Abstractions.csproj" />
  </ItemGroup>

</Project>
