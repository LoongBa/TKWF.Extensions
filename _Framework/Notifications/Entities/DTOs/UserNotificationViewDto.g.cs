// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: 57deaada7b5c726ea6e481cb9ad6bf51879651812e76938a3ae49def34360bab]
// 模型: UserNotificationView (TKWF.Ext.Notifications.UserNotificationView)
// 生成时间: 2026-09-12 00:30:07
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.Notifications;

namespace TKWF.Ext.Notifications.DTOs;
/// <summary>用户通知视图实体（VEntity）——跨表 JOIN <c>UserNotification</c> → <c>Notification</c>，按 UserId + 通知名单查询分页。     <para>V0.2.0：替代两步查询（先按 name 取 Notification.Id 集合，再按集合过滤收件箱）——JOIN 下推 DB，     单查询完成跨表过滤 + 分页 + 排序，顺带返回通知名/严重级别/显示名（GraphQL 路径可用）。     VEntity 只读：<c>IDomainViewEntity</c> 由 SG1 自动生成，禁 IEntityDAC 写操作。</para></summary>
public partial record UserNotificationViewDto : IDomainDto<UserNotificationView>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键——透传 UserNotification.Id（每收件箱行一条，唯一稳定）。 </summary>
    public long Id { get; init; }
    /// <summary> 收件人用户 ID（外层过滤键，视图外参数化）。 </summary>
    public long UserId { get; init; }
    /// <summary> 关联 Notification.Id。 </summary>
    public long NotificationId { get; init; }
    /// <summary> 已读状态（0=Unread，1=Read）。 </summary>
    public int State { get; init; }
    /// <summary> 已读时间（未读为 null）。 </summary>
    public DateTime? ReadTime { get; init; }
    /// <summary> 创建时间（UTC，透传 UserNotification.CreateTime）。 </summary>
    public DateTime CreateTime { get; init; }
    /// <summary> 通知名（稳定唯一，外层过滤键——WHERE Name = @name）。 </summary>
    public required string Name { get; init; }
    /// <summary> 严重级别（NotificationSeverity 枚举值，GraphQL 路径可用）。 </summary>
    public int Severity { get; init; }
    /// <summary> 通知显示名快照（GraphQL 路径可用）。 </summary>
    public string? DisplayName { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static UserNotificationViewDto FromEntity(UserNotificationView entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new UserNotificationViewDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            UserId = entity.UserId,
            NotificationId = entity.NotificationId,
            State = entity.State,
            ReadTime = entity.ReadTime,
            CreateTime = entity.CreateTime,
            Name = entity.Name,
            Severity = entity.Severity,
            DisplayName = entity.DisplayName,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<UserNotificationView, UserNotificationViewDto>> SelectExpression => e => new UserNotificationViewDto
    {
        Id = e.Id,
        UserId = e.UserId,
        NotificationId = e.NotificationId,
        State = e.State,
        ReadTime = e.ReadTime,
        CreateTime = e.CreateTime,
        Name = e.Name,
        Severity = e.Severity,
        DisplayName = e.DisplayName,
    };
    #endregion

    UserNotificationView IDomainDto<UserNotificationView>.ApplyToEntity(UserNotificationView entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(UserId), d => d.UserId, (e, v) => e.UserId = v )
            .Map(nameof(NotificationId), d => d.NotificationId, (e, v) => e.NotificationId = v )
            .Map(nameof(State), d => d.State, (e, v) => e.State = v )
            .Map(nameof(ReadTime), d => d.ReadTime, (e, v) => e.ReadTime = v )
            .Map(nameof(CreateTime), d => d.CreateTime, (e, v) => e.CreateTime = v )
            .Map(nameof(Name), d => d.Name, (e, v) => e.Name = v )
            .Map(nameof(Severity), d => d.Severity, (e, v) => e.Severity = v )
            .Map(nameof(DisplayName), d => d.DisplayName, (e, v) => e.DisplayName = v )
        ;
        return entity;
    }

    UserNotificationView IDomainDto<UserNotificationView>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(UserNotificationViewDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<UserNotificationView>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<UserNotificationView, UserNotificationViewDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class UserNotificationViewDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static UserNotificationViewDto ToDto(this UserNotificationView entity) => UserNotificationViewDto.FromEntity(entity);
}

