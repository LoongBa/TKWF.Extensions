// <auto-generated>
// 此文件由 xCodeGen 自动生成，请勿手动修改。
// @[xCodeGen.Hash: d570e7b3c7e252bbc15c5caf98c3529a665aa007c4f96fefc23bce9e9649afaf]
// 模型: PermissionGrantEntity (TKWF.Ext.Permissions.PermissionGrantEntity)
// 生成时间: 2026-09-14 08:08:20
// </auto-generated>
#nullable enable
using System;
using System.Linq;
using System.Linq.Expressions;
using System.Collections.Generic;
using System.ComponentModel.DataAnnotations;
using TKW.Framework;
using TKW.Framework.CodeGeneration;
using TKW.Framework.Domain;
using TKW.Framework.Domain.Interfaces;
using TKW.Framework.Domain.Exceptions;
using TKW.Framework.Domain.xCodeGen;
using TKWF.Ext.Permissions;

namespace TKWF.Ext.Permissions.DTOs;
/// <summary>条件工厂（V4.9）。支持两种使用方式：     <list type="bullet">     <item><c>PermissionGrantEntity.Conditions.ByXxx(value)</c> — 直接返回表达式（已有代码兼容）</item>     <item><c>PermissionGrantEntity.Where.ByXxx(value).ByYyy(value)</c> — 链式组合，隐式转换到 Expression</item>     </list></summary>
public partial record PermissionGrantEntityDto : IDomainDto<PermissionGrantEntity>
{
    [System.Text.Json.Serialization.JsonIgnore]
    public bool IsFromPersistentSource { get; set; }

    #region 属性定义
    /// <summary> 主键（自增）。 </summary>
    public long Id { get; init; }
    /// <summary> 权限名（如 "Order.Create"）。 </summary>
    public required string PermissionName { get; init; }
    /// <summary> 授予 provider（如 "User"/"Role"/"Member"）。 </summary>
    public required string ProviderName { get; init; }
    /// <summary> provider 键（如 UserIdString）。 </summary>
    public required string ProviderKey { get; init; }
    /// <summary> 是否授予。 </summary>
    public bool IsGranted { get; init; }
    #endregion

    #region [ 静态工厂 ]
    /// <summary> 从实体转换为 DTO </summary>
    public static PermissionGrantEntityDto FromEntity(PermissionGrantEntity entity)
    {
        ArgumentNullException.ThrowIfNull(entity);
        return new PermissionGrantEntityDto
        {
            IsFromPersistentSource = entity.IsFromPersistentSource,
            Id = entity.Id,
            PermissionName = entity.PermissionName,
            ProviderName = entity.ProviderName,
            ProviderKey = entity.ProviderKey,
            IsGranted = entity.IsGranted,
        };
    }

    /// <summary>
    /// 查询投影表达式（供 DataService 无投影重载使用，保留 SQL 字段裁剪）。
    /// 必须是纯 MemberInit 表达式（new DTO { ... }）——FromEntity 是方法调用，FreeSql 无法翻译成 SQL；
    /// 且不得包含 IsFromPersistentSource（非业务列），由框架在取数后统一置位。
    /// </summary>
    public static Expression<Func<PermissionGrantEntity, PermissionGrantEntityDto>> SelectExpression => e => new PermissionGrantEntityDto
    {
        Id = e.Id,
        PermissionName = e.PermissionName,
        ProviderName = e.ProviderName,
        ProviderKey = e.ProviderKey,
        IsGranted = e.IsGranted,
    };
    #endregion

    PermissionGrantEntity IDomainDto<PermissionGrantEntity>.ApplyToEntity(PermissionGrantEntity entity, EnumSceneFlags scene)
    {
        ArgumentNullException.ThrowIfNull(entity);
        this.CreateMapper(entity, scene)
            .Map(nameof(PermissionName), d => d.PermissionName, (e, v) => e.PermissionName = v )
            .Map(nameof(ProviderName), d => d.ProviderName, (e, v) => e.ProviderName = v )
            .Map(nameof(ProviderKey), d => d.ProviderKey, (e, v) => e.ProviderKey = v )
            .Map(nameof(IsGranted), d => d.IsGranted, (e, v) => e.IsGranted = v )
        ;
        return entity;
    }

    PermissionGrantEntity IDomainDto<PermissionGrantEntity>.ToEntity(EnumSceneFlags scene)
    {
        throw new NotSupportedException($"ToEntity not implemented for {nameof(PermissionGrantEntityDto)}. Create entity manually or override in DTO extension file.");
    }

    void IDomainDto<PermissionGrantEntity>.ValidateData(EnumSceneFlags scene)
    {
        if (IsFromPersistentSource && (scene & EnumSceneFlags.ForceValidate) == 0) return;
        
        var results = ValidateDataCore(scene).ToList();
        OnCustomValidate(scene, results);
        if (results.Any()) throw new ValidationResultsException(results);
    }

    protected IEnumerable<ValidationResult> ValidateDataCore(EnumSceneFlags scene)
    {
        var results = new List<ValidationResult>();
        this.CreateDtoValidator<PermissionGrantEntity, PermissionGrantEntityDto>(scene, results)
        ;
        return results;
    }

    partial void OnCustomValidate(EnumSceneFlags scene, List<ValidationResult> results);

    /// <summary>代码生成器标识</summary>
    public const string Generator = "xCodeGen:Dto.cshtml";
    public const string GeneratorVersion = "4.9";
}

/// <summary>Entity 转 DTO 扩展方法</summary>
public static class PermissionGrantEntityDtoExtensions
{
    /// <summary> 将 Entity 转换为 DTO </summary>
    public static PermissionGrantEntityDto ToDto(this PermissionGrantEntity entity) => PermissionGrantEntityDto.FromEntity(entity);
}

