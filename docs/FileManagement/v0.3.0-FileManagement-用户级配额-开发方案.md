# v0.3.0 — FileManagement 用户级配额 开发方案

> **扩展**: `TKWF.Ext.FileManagement` | **版本**: v0.3.0 | **依赖**: 主框架 v4.10.26+（ADR78 签名）+ 既有 v0.2.0 版本化/配额
> **日期**: 2026-09-16 | **状态**: 📋 提议（修订 1——Oracle 评审 PASS WITH CONDITIONS 11 条件已吸收，待实施）
> **关联**: 跟踪 §五 #5（ADR 明示"留后续"，现仅目录/全局配额）；README v0.3.0+ 候选（`OwnerId` 列 + `IDomainUser` 注入）；Settings/FeatureManagement `IDomainUser` 注入先例

---

## 一、目的与目标

为 FileManagement 增加**用户级配额**——每用户文件数/容量独立限制，超越现有目录级（`MaxFilesPerFolder`/`MaxFolderSizeBytes`）与全局级（`MaxTotalSizeBytes`）配额。上传时经 `IDomainUser` 解析当前用户，按 `OwnerId` 维度检查用户配额，超限拒绝。

目标状态（3 句内）：①`ManagedFileEntity` 增加 `OwnerId` 列（可空——兼容 v0.2.0 存量数据，匿名/无用户上下文场景为空）；②新增 `MaxUserFilesCount`/`MaxUserSizeBytes` 配置键，`FileManager.EnforceQuotaAsync` 增加用户维度检查（经 `IDomainUser` 解析 owner）；③`OwnerId` 写入上传链（`UploadFileAsync`），存量文件不回填（`OwnerId` null = 未归属，不计入用户配额）。

---

## 二、问题

### 问题现象

1. **多用户场景配额缺失**：现配额只有目录维度（`MaxFilesPerFolder`/`MaxFolderSizeBytes`）和全局维度（`MaxTotalSizeBytes`）——同一用户可无限制上传至全局上限（如 10GB），SaaS/多租户场景无法按用户限流（"每人 1GB"）。
2. **无归属维度**：`ManagedFileEntity` 无 `OwnerId` 列——文件无法归属到上传用户，用户维度统计/配额无从谈起。
3. **README 明示缺口**：v0.3.0+ 候选明确列出"用户级配额（`OwnerId` 列 + `IDomainUser` 注入）"——跟踪 §五 #5 记录"ADR 明示留后续，现仅目录/全局配额"。

### 触发场景

- 消费方多用户文件管理（如网盘/文档中心）——每位用户独立容量上限。
- 目录级配额不适用时（用户文件散落多目录，需跨目录按用户汇总）。
- 匿名上传（无用户上下文）——`OwnerId` null，用户配额不生效（保持全局配额兜底）。

### 现有方案不足

- 目录配额（`MaxFolderSizeBytes`）按 `FolderId` 聚合——用户可创建多目录分散文件绕过。
- 全局配额（`MaxTotalSizeBytes`）无用户维度——无法区分"谁占用了容量"。
- 两者均不回答"某用户已用多少、还剩多少"——用户级配额是**归属维度**的新增，非现有键的扩展。

---

## 三、使用场景

1. **多用户网盘**（常态）：消费方启用 `[TKWFEnabledExtension(FileManagement)]`，配置 `MaxUserSizeBytes: 1073741824`（每人 1GB）——用户 A 上传至 1GB 后超限拒绝，用户 B 独立计算不受影响。
2. **匿名/系统场景**：`IDomainUser` 未认证（`IsAuthenticated=false`）或上传不在域作用域 → `OwnerId` 写 null → 用户配额跳过，全局配额兜底（与现状一致）。
3. **不适用**：单用户应用（无用户配额需求，保持目录/全局配额即可，配置缺省行为不变）；`OwnerId` 不回填存量数据（历史文件不计入用户配额——设计决策，见【后果】）。

---

## 四、决策

### 核心机制（对齐 Settings/FeatureManagement `IDomainUser` 注入先例）

```
上传链（UploadFileAsync）
  ├─ IDomainUser 解析 owner（构造注入 + Initializer 工厂）
  │    ├─ IsAuthenticated + UserId 非空 → OwnerId = UserId（long?）
  │    └─ 否则 → null（匿名，跳过用户配额）
  ├─ EnforceQuotaAsync 增加用户维度检查（MaxUserFilesCount / MaxUserSizeBytes）
  │    └─ CountByOwnerAsync / SumSizeByOwnerAsync（DataService 聚合，对齐 SumSizeByFolderIdAsync 先例）
  └─ 元数据落库 → ManagedFileEntity.OwnerId
```

### 1. 实体：`ManagedFileEntity` 增加 `OwnerId` 列

- **列定义**（`ManagedFileEntity.cs` 实体声明，`.g.cs` 重新生成）：
  ```csharp
  /// <summary>归属用户 ID（V0.3.0 用户级配额；null = 匿名/未归属，不计入用户配额；写入后不可变）。</summary>
  [FreeSql.DataAnnotations.Column(Position = 12, IsNullable = true, CanUpdate = false)]
  public long? OwnerId { get; set; }
  ```
  - **⚠️ Position = 12**（Oracle 评审 BLOCKER 修正）：`ManagedFileEntity.cs` Position 1-11 已全部占用（Position=5 为 `Extension` 列）——新列追加末尾（对齐增量 ALTER TABLE 最稳）。
  - **CanUpdate = false**（Oracle 条件 2 定案"所有权保留"）：`OwnerId` 一经写入不可变——版本化 Upsert/回滚均不动它，权益语义清晰（谁首传谁拥有）。
- **索引**：类头新增（与 `IX_ManagedFile_Sha256` 同级）：
  ```csharp
  [FreeSql.DataAnnotations.Index("IX_ManagedFile_Owner", nameof(OwnerId))]
  ```
- **兼容**：`OwnerId` 可空 → 建表增量迁移（FreeSql SyncTables）；存量行 null（不回填，见【后果】）。
- **⚠️ 生成物纪律**：实体变更后重跑 `pwsh .xCodeGen/run-xcodegen.ps1 -Ext FileManagement` 更新 `.g.cs`（AGENTS §8 重生成纪律）——`OwnerId` 属性将进入 DTO/DataService 映射；**测试计划含 "`.g.cs` 含 `OwnerId` 映射"断言**（Oracle 条件 8）。

### 2. Options：新增用户配额键

`FileManagementOptions` 增加（对齐 V0.2.0 三键可空语义）：

```csharp
/// <summary>用户文件数上限（null = 不限制；OwnerId 归属文件计数；超限上传 → InvalidOperationException）。</summary>
public int? MaxUserFilesCount { get; set; }

/// <summary>用户容量上限（字节；null = 不限制；OwnerId 归属文件大小求和；超限上传 → InvalidOperationException）。</summary>
public long? MaxUserSizeBytes { get; set; }
```

### 3. Store/DataService：用户维度聚合

- `ManagedFileEntityDataService` 增加 2 个聚合方法（对齐 `CountByFolderIdAsync`/`SumSizeByFolderIdAsync` 先例——SQL 下推）：
  ```csharp
  public Task<long> CountByOwnerAsync(long ownerId, CancellationToken ct)      // COUNT WHERE owner_id = @id
  public Task<long> SumSizeByOwnerAsync(long ownerId, CancellationToken ct)    // SUM(size) WHERE owner_id = @id
  ```
- `IManagedFileStore` 增加对应委托方法（internal Store → DataService 转发）。

### 4. FileManager：IDomainUser 注入 + 用户配额检查

- **构造注入** `IDomainUser`（对齐 FeatureManagement FeatureManager 先例——`private readonly IDomainUser _domainUser`）。
- **Initializer 工厂**改为传入 `sp.GetRequiredService<IDomainUser>()`（对齐 FeatureManagementExtensionInitializer L81 先例）。
- **所有权语义（Oracle 条件 2 定案——所有权保留）**：
  - `OwnerId` **仅在新建文件分支写入**（`UploadFileAsync` 创建 `ManagedFileEntity` 时 `OwnerId = ownerId`）；
  - **版本化 Upsert 新版本分支不动 OwnerId**（新上传者不夺走所有权——文件归首传者，新版本字节计入原 owner 配额，`SumSizeByOwnerAsync` 按主表 `Size` 单指针求和不重复计版本行）；
  - **RollbackFileAsync 不动 OwnerId**（回滚是指针切换，非新上传，Oracle 条件 3）。
- **`UploadFileAsync` 内 `EnforceQuotaAsync` 增加用户维度**（Oracle 条件 7——**检查顺序契约：用户计数 → 用户容量 → 目录计数 → 目录容量 → 全局容量**；在现有方法顶部插入用户块、其余 3 键不动）：
  ```csharp
  // 用户配额（Oracle 定案：OwnerId 归属维度；未认证/系统账号/非数值 UserId → 跳过，全局兜底）
  long? ownerId = (_domainUser.IsAuthenticated && !_domainUser.IsSystemActor)
      ? long.TryParse(_domainUser.UserId, out var uid) ? uid : null
      : null;
  if (ownerId.HasValue)
  {
      if (_options.MaxUserFilesCount.HasValue)
      {
          long count = await _fileStore.CountByOwnerAsync(ownerId.Value, ct);
          if (count >= _options.MaxUserFilesCount.Value)
              throw new InvalidOperationException($"用户文件数配额超限：当前 {count} / 上限 {_options.MaxUserFilesCount.Value}");
      }
      if (_options.MaxUserSizeBytes.HasValue)
      {
          long current = await _fileStore.SumSizeByOwnerAsync(ownerId.Value, ct);
          if (current + contentLength > _options.MaxUserSizeBytes.Value)
              throw new InvalidOperationException($"用户容量配额超限：当前 {current} 字节 + 本次 {contentLength} 字节 > 上限 {_options.MaxUserSizeBytes.Value} 字节");
      }
  }
  ```
  - **⚠️ 非数值 UserId 约束（Oracle 条件 5）**：`long.TryParse` 失败 → `OwnerId` null → 用户配额静默跳过（全局兜底）——**用户配额仅支持数值型 UserId**（GUID/字符串 ID 消费方用户配额不生效）；首次失败 `_logger.LogWarning` 一次告警便于排查。
- **元数据写入**：新建文件分支 `OwnerId = ownerId`；版本化/回滚分支不动；匿名/系统/非数值 → null。

### 5. 破坏性声明

- **实体加列**：`ManagedFileEntity` 增 `OwnerId`（可空，Position=12）→ 建表增量迁移（FreeSql SyncTables）；存量行 null。
- **DTO 变化**：`ManagedFileEntityDto` 增 `OwnerId`（SG1 重新生成）——消费方读侧零破坏（加性）；写侧 DTO 增字段不影响既有调用。
- **构造注入变化**：`FileManager` 构造函数增 `IDomainUser` 参数——**internal 构造**（仅 Initializer 工厂调用），消费方自定义 `IFileManager` 不受影响；**非推荐直 new 路径（反射/ActivatorUtilities）须同步增 `IDomainUser` 参数**（Oracle 条件 11）。`FileManagementExtensionInitializer` 工厂内部改动。
- **行为变化**：匿名上传（无用户上下文）→ `OwnerId` null → 用户配额不生效（全局兜底）——与现状一致，零破坏。
- **测试 Host**：`FileManagementTestHost` **已注册 `StubDomainUser`**（DataService 早已用）——加 `IDomainUser` 到 `FileManager` 构造零破坏；但 `StubDomainUser` 当前 `IsAuthenticated=false`/`UserId=null`，**须参数化支持认证场景**（Oracle 条件 9，见测试计划）。

### 5.5 ADR 落档（Oracle 条件 10）

按 AGENTS §4（扩展间协议变更 + 关键设计取舍），本方案立 ADR：
`docs/FileManagement/ADR/ADR-FileManagement-用户级配额所有权语义.md`（三问必填）——记录：Position=12 选择、**所有权保留**决策（CanUpdate=false + 新版本/回滚不动 OwnerId）、非数值 UserId 降级约束、删除即释放配额语义。

---

## 五、测试计划

| 层 | 测试 | 断言 |
|----|------|------|
| DataService | `CountByOwnerAsync`/`SumSizeByOwnerAsync` SQL 聚合（SQLite 内存） | 按 OwnerId 过滤正确；无归属（null）不计入 |
| FileManager | 用户文件数配额（`MaxUserFilesCount`） | 达上限后上传 → `InvalidOperationException`（含配额信息） |
| FileManager | 用户容量配额（`MaxUserSizeBytes`） | 当前用量 + 本次 > 上限 → 拒绝；等值边界（= 允许） |
| FileManager | 匿名上传（`IDomainUser` 未认证） | `OwnerId` null → 用户配额跳过，全局配额仍生效 |
| FileManager | 系统账号上传（`IsSystemActor`） | `OwnerId` null → 用户配额跳过（Oracle 条件 6） |
| FileManager | 非数值 UserId（GUID/字符串） | 用户配额静默跳过（不抛），全局兜底 + Warning 日志（Oracle 条件 5） |
| FileManager | 多用户隔离 | 用户 A 达配额不影响用户 B 上传 |
| FileManager | **版本化分支 OwnerId 不变**（所有权保留） | 用户 B 上传同名不同内容 → 主表 `OwnerId` 仍为用户 A（Oracle 条件 8） |
| FileManager | **回滚分支 OwnerId 不变** | `RollbackFileAsync` 后 `OwnerId` 不动（Oracle 条件 8） |
| FileManager | **删除后配额释放** | `DeleteFileAsync` 后 `SumSizeByOwnerAsync` 不含该文件（Oracle 条件 4） |
| 实体 | `OwnerId` 列持久化 | 上传后 `OwnerId` 正确落库；存量数据 null 兼容；`CanUpdate=false`（Update 尝试不改） |
| 生成物 | **`.g.cs` 含 `OwnerId` 映射** | 重跑 `run-xcodegen.ps1 -Ext FileManagement` 后 DTO/DataService 含 `OwnerId`（AGENTS §8 #4，Oracle 条件 8） |
| 回归 | 既有 v0.2.0 配额（目录/全局） | 用户级新键缺省（null）时行为不变 |

> **测试 Host 改造（Oracle 条件 9）**：`StubDomainUser` 增参数化构造（`userId`、`isAuthenticated`、`isSystemActor`）——默认匿名桩保持现状（既有测试零改动），用户配额测试传认证桩。

## 六、风险与缓解

| 风险 | 缓解 |
|------|------|
| `IDomainUser.UserId` 解析（string → long）失败 | `long.TryParse` 失败 → null → 跳过用户配额（全局兜底）+ **首次失败 Warning 日志**（Oracle 条件 5）；使用指南明示"用户配额仅支持数值型 UserId" |
| 并发"先到先得"（读-比-写非原子） | 对齐 v0.2.0 配额语义（显式声明，超限最终一致可接受） |
| 存量文件不计入用户配额 | 设计决策（不回填）——文档化：启用用户配额前已有文件不占额度，新上传开始计入 |
| 实体加列生成物陈旧 | 重跑 `run-xcodegen.ps1 -Ext FileManagement` 提交 `.g.cs`（AGENTS §8 纪律）+ 测试断言映射（Oracle 条件 8） |
| `IDomainUser` 注入破坏测试 Host | **已注册（零破坏）**——仅需参数化 `StubDomainUser` 支持认证场景（Oracle 条件 9，修正初稿"补注册"措辞） |
| 所有权语义歧义（新版本谁付费） | **所有权保留**定案（Oracle 条件 2）——`OwnerId` 仅新建写入，新版本计入原 owner；ADR 记录 |

## 七、Oracle 评审状态

| 轮次 | 结论 | 说明 |
|------|------|------|
| 一次（bg_670b41d1） | **PASS WITH CONDITIONS** | 11 条件全部吸收：①**Position 修正 5→12**（BLOCKER：Position=5 与 `Extension` 列冲突，Oracle 核实）②**所有权保留定案**（仅新建分支写 OwnerId，CanUpdate=false）③Rollback 不动 OwnerId ④删除即释放配额明示 ⑤非数值 UserId 文档化 + Warning ⑥IsSystemActor 排除 ⑦检查顺序契约明示 ⑧测试计划补 4 项（版本化/回滚/非数值/生成物断言）⑨StubDomainUser 参数化 ⑩补 ADR ⑪破坏性声明补直 new 路径 |

---

## 变更记录

| 日期 | 状态 | 说明 |
|------|------|------|
| 2026-09-16 | 📋 提议 | 初稿——基于勘察（README v0.3.0+ 候选 + 跟踪 §五 #5 + Settings/FeatureManagement `IDomainUser` 先例 + 现有配额链 EnforceQuotaAsync）；待 Oracle 评审 |
| 2026-09-16 | 📋 提议（修订 1） | **Oracle 评审 PASS WITH CONDITIONS（bg_670b41d1）11 条件全部吸收**：Position 5→12（BLOCKER）、所有权保留定案（CanUpdate=false + 新版本/回滚不动）、IsSystemActor 排除、非数值 UserId 文档化 + Warning、检查顺序契约、测试计划补 4 项、StubDomainUser 参数化、ADR 补立、破坏性声明补直 new 路径 |
