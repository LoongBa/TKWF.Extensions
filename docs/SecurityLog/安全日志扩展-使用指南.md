# SecurityLog 安全日志扩展-使用指南

> 扩展：`TKWF.Ext.SecurityLog`（v0.2.0）——认证/授权安全事件记录与查询（登录成败/登出/改密/重置/锁定/注册/挑战 + IP/UA/结果/详情）+ v0.2.0 异常检测聚合与保留天数清理。
> 本指南公开；迭代开发方案/ADR/审核报告见主框架私有 `_TKWF/docs/03_扩展模块/SecurityLog/`。

---

## 1. 本扩展解决什么

主框架审计日志（`TKWF.Ext.AuditLogging`）记录**方法调用事件**（业务操作追踪），**无安全事件维度**（EventType/IP/UserAgent/Result）——登录失败来源 IP、暴力破解轨迹、改密操作无法从 AuditLog 回答。

本扩展提供：
- **独立安全事件实体** `SecurityLog`（SG1）：登录/登出/改密/密码重置/锁定/注册/挑战 + IP/UA/结果/详情/关联 ID；
- **自动采集**（Domain AOP 过滤器）：拦截 `AuthController` 安全方法与 `IPasswordResetFlow`，无需改主框架，消费方 opt-in；
- **查询 API**：按用户/IP/事件类型/结果/时间分页检索 + 计数；
- **异常检测聚合**（v0.2.0）：失败次数 TopN by 用户名/来源 IP（暴力破解/撞库分析）；
- **保留天数清理**（v0.2.0）：过期日志按留存窗口分批物理删除。

**与相关扩展划界**：
- **AuditLogging** = 方法调用审计（业务操作追踪）；**SecurityLog** = 安全事件记录（认证/授权，含 IP/UA/结果）——两者并存、语义不同。
- **Account** V0.3.0「登录历史与异常检测」将**消费本扩展查询**（SecurityLog 为唯一数据源），Account 不重复建表。

## 2. 安装

```xml
<ProjectReference Include="..\..\_Framework\SecurityLog\TKWF.Ext.SecurityLog.csproj" />
```

## 3. 启用 + 接线

```csharp
// ① 白名单启用（v4.9.85+：发现不自动启用）
[TKWFEnabledExtension(typeof(SecurityLogExtensionInitializer<>))]
public class MyDomainInitializer : DomainHostInitializerBase<MyUserInfo>
{
    // ② 采集接线：全局注册安全日志过滤器（opt-in——只拦安全方法，白名单判定）
    protected override void ConfigureGlobalFilters(FilterBuilder<MyUserInfo> builder)
        => builder.AddSecurityLog();
}
```

> 过滤器经 Domain AOP 管线拦截 `AuthController` 安全方法与 `IPasswordResetFlow` 实现（CanWeGo 白名单：目标类型 `IAuthController`/`IPasswordResetFlow` + 8 个方法名）；成功/失败自动判定，异常（`AuthenticationException`）记 Failed，其他异常不拦截（保持原语义）。

## 4. 配置（`TKWF:SecurityLog`）

```jsonc
{
  "TKWF": {
    "SecurityLog": {
      "Enabled": true,                        // false = 过滤器零开销（不采集）
      "EventTypes": [                         // 启用的事件类型（默认全部）
        "Login", "Logout", "PasswordChange", "PasswordReset",
        "Lockout", "Register", "Challenge"
      ],
      "RetentionDays": 90,                    // v0.2.0 保留天数清理：删除 CreateTime < UtcNow-RetentionDays 的过期记录（默认 90）
      "CleanupBatchSize": 500                 // v0.2.0 每批删除条数——分批清理防长事务（默认 500）
    }
  }
}
```

## 5. 事件类型

| EventType | 触发 | Result 判定 |
|-----------|------|------------|
| `Login` | `LoginByPasswordAsync`/`LoginByContextAsync` | 成功 = 正常返回；失败 = 捕获 `AuthenticationException` 记 Failed（含 Detail 脱敏异常消息） |
| `Logout` | `LogoutAsync` | 正常返回即 Success |
| `PasswordChange` | `ChangePasswordSecureAsync` | 成功/失败（旧密码错误等） |
| `PasswordReset` | `InitiateResetAsync`/`CompleteResetAsync`（`IPasswordResetFlow` 实现） | 成功/失败（`ResetResult.Success`） |
| `Lockout` | `LoginByContextAsync` 锁定分支 | 异常消息含「锁定」判定 Failed（v0.1.0 语义） |
| `Register` | `RegisterSecureAsync` | 成功/失败（`RegisterResult.Success`） |
| `Challenge` | `RequestChallengeAsync` | 成功/失败 |

> `EventCategory` 统一 `Authentication`（v0.1.0 仅认证类事件；`Authorization` 值预留）。
> **防枚举语义**：登录失败记录**尝试用户名**（请求输入）——安全审计需要来源，不因防枚举丢失。
> v0.2.0 起事件类型/结果/分类字符串常量收敛于 `SecurityLogEventTypes` 静态类（采集过滤器已引用，消费方组装判定时可复用）。

## 6. 查询

```csharp
public class SecurityQueryService(ISecurityLogQueryService query)
{
    public async Task<SecurityLogPagedResult> SearchAsync(string? ip = null, string? type = null)
        => await query.GetListAsync(new SecurityLogQueryInput(
            IpAddress: ip, EventType: type, Take: 50));
}
```

- `GetListAsync(input)`：分页过滤（UserName/IpAddress/EventType/Result/时间范围；Take 默认 50 上限 200）；
- `GetDetailAsync(id)`：详情（含 Detail 全文）；
- `CountAsync(input)`：计数（异常检测/仪表盘用）。

> **安全决策**：列表 DTO **不含 Detail**（异常消息/敏感摘要不随列表拉取），详情按 Id 取——对齐 AuditLogging 不含 ArgumentsJson 先例。

## 7. 异常检测聚合（v0.2.0）

```csharp
public class SecurityOpsService(ISecurityLogAnalyticsService analytics)
{
    // 暴力破解/撞库检测：近 10 分钟失败次数 Top10 用户名（疑似撞库目标）与来源 IP（疑似爆破源）
    public async Task<IReadOnlyList<SecurityLogFailureStat>> SuspiciousAsync()
        => await analytics.GetTopFailedUsersAsync(topN: 10, window: TimeSpan.FromMinutes(10));
}
```

- `GetTopFailedUsersAsync(topN=10, window=null)`：窗口内失败次数 TopN（按**尝试用户名**）；`window` 为 `TimeSpan` 相对窗口（`[UtcNow-window, UtcNow]`），null = 全量；仅计 `Result=="Failed"` 记录，空白 UserName 跳过。
- `GetTopFailedIpsAsync(topN=10, window=null)`：同上按**来源 IP**；IpAddress 为 null/空白跳过（无来源 IP 的失败不构成 IP 维度）。
- 输出 `SecurityLogFailureStat(Dimension, Count)`——`Dimension` = 用户名/IP，`Count` = 窗口内失败次数，按 Count 降序。
- **实现**：DataService 内 SQL WHERE 下推（Result + 时间范围，利用 `IX_SecurityLog_UserEventTime` / `IX_SecurityLog_IpTime` 索引）+ 内存 GroupBy（`Take(100_000)` 安全上限，对齐 Tagging 聚合范式——IQueryable 桥接不支持 GroupBy 翻译）。聚合**只读**。
- 异常静默：聚合失败仅 Warning 日志 + 空结果，不阻断告警管线。

## 8. 保留天数清理（v0.2.0）

```csharp
public class DailyCleanupJob(ISecurityLogAnalyticsService analytics)
{
    // 每日清理：删除 CreateTime < UtcNow - RetentionDays(默认 90) 的过期记录，返回删除条数
    public Task<int> CleanAsync(CancellationToken ct = default)
        => analytics.CleanupExpiredAsync(ct);
}
```

- 配置：`RetentionDays`（默认 90）/ `CleanupBatchSize`（默认 500）——见 §4 配置节。
- `CleanupExpiredAsync()`：按 `batchSize` **分批循环**删除过期记录（每批独立查询 + 删除，防长事务/大锁），返回实际删除总条数；适合每日后台任务调度。
- **决策记录（打破"只增不改"语义）**：v0.1.0 安全日志为只写流水、不可删除；v0.2.0 保留清理引入**物理删除**——属合规留存窗口（默认 90 天）的运维必需。限定为 DataService `DeleteExpiredAsync` 单点物理删（`hasSoftDelete:false`，绝不走 `EntitySoftDeleteAsync`）、无管理端点/单条删除、`CreateTime` 仍 `CanUpdate=false`。**主框架 ADR 待用户许可后补录**（此处 + 代码注释承担决策记录职责）。

## 9. 安全与边界

- **Detail 脱敏**：仅记异常消息（不含密码/令牌/完整堆栈）；SensitiveFields 对齐 AuditLog 语义。
- **只增不改（v0.1.0 语义 + v0.2.0 例外）**：SecurityLog 无任何 Update/单条删除 API；唯一删除路径 = §8 保留清理（过期记录物理删，限定单点）。
- **异常静默**：落库/查询/聚合/清理失败仅 Warning 日志，不阻断登录/改密/告警流程。
- **不实现**：实时推送（WebSocket/SignalR，超出后端扩展范围）；Alter 主框架。

## 变更记录

| 日期 | 版本 | 变更内容 |
|------|------|---------|
| 2026-09-11 | v0.2.0 | 异常检测聚合（`ISecurityLogAnalyticsService`——失败次数 TopN by 用户名/来源 IP，内存 GroupBy 范式）+ 保留天数清理（`RetentionDays`/`CleanupBatchSize`，分批物理删，打破"只增不改"的决策记录见 §8）+ `SecurityLogEventTypes` 字符串常量收敛（过滤器重构，行为零变更） |
| 2026-09-09 | v0.1.0 | 初始版本——独立 SecurityLogEntity + Domain AOP 过滤器采集（opt-in 白名单）+ 查询 API + 划界（AuditLogging 方法调用 / Account 登录历史消费本扩展） |
