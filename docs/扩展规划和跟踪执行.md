# 扩展规划和跟踪执行

> 本文件是 TKWF.Extensions 仓库的**扩展路线图 + 执行跟踪唯一入口**。
> 所有扩展（已有/规划/候选）的进度、版本、状态在此登记。

---

## 一、扩展总览

| 扩展 | 包名 | 状态 | 版本 | 说明 |
|------|------|:----:|:----:|------|
| **Tags** | `TKWF.Ext.Tagging` | ✅ 已迁出 | v4.9.79 | 标签提取/匹配/格式化（从框架核心迁出，D17 模式 3） |
| **Permissions** | `TKWF.Ext.Permissions` | 🔵 主框架内（V4.9.72） | — | 权限定义/检查/存储/过滤——迁出候选（主框架 slnx 中） |
| **Navigation** | `TKWF.Ext.Navigation` | 🔵 主框架内（V4.9.74） | — | 菜单定义/管理器/权限过滤——迁出候选（主框架 slnx 中） |

**状态图例**：✅ 已迁出（独立本仓库）/ 🔵 主框架内（待迁出）/ 🟡 规划中 / ⚪ 候选 / ⛔ 否决

---

## 二、已迁出扩展（本仓库开发）

### Tags（TKWF.Ext.Tagging）✅ 已迁出

| 项 | 值 |
|----|----|
| 迁出版本 | v4.9.79（主框架 commit b3ffa05） |
| 仓库位置 | `_Framework/Tagging/` + `_Tests/Extension.Tagging.Tests/` |
| 技术规范 | `_Framework/Tagging/README.md`（原「TKWF.Tools.Tags 标签提取引擎」） |
| 功能 | `ITagService` + `TagService`（门面）+ `ITokenizer`（分词）+ 匹配器家族 + `TagExtractionPipeline`（流水线）+ 后处理器（互斥/默认兜底）+ `TaggingExtensionInitializer`（三钩子接线） |
| 测试 | 4 例（Extension.Tagging.Tests） |
| 演进路线 | README §六：词典批量（AC 自动机）/ 邻近度 / 逻辑组合 / 模糊纠错 / 语义向量匹配——后续迭代逐个落地 |

#### Tags 待办

- [ ] 调整 README 内容（用户指示"下一步再调整内容"）
- [ ] 配置结构验证接入（`[Options]`，对齐 V4.9.76 D1 机制）
- [ ] 匹配器演进路线逐项评估（AC 自动机优先——高并发词库场景）

---

## 三、迁出候选扩展（主框架 slnx 内，V4.9.77 后迁出）

> 以下扩展已在主框架完成核心实现（V4.9.72-76），按"扩展独立仓库"新规逐步迁出本仓库。
> 迁出时：从主框架 slnx 移除 → 移入本仓库 `_Framework/{扩展名}` + `_Tests/` → 调整 `$(TKWFRoot)` 引用 → 保留 git 历史（主框架侧 rm + 新仓库 add）。

### Permissions（TKWF.Ext.Permissions）🔵 迁出候选

| 项 | 值 |
|----|----|
| 主框架版本 | V4.9.72（tag 已打）+ V4.9.75 FreeSqlPermissionStore + V4.9.76 PermissionOptions |
| 功能 | `PermissionDefinition` + 贡献者 + `RequirePermission` 过滤器（fail-closed）+ `PermissionChecker<TUserInfo>` + `IPermissionStore`/`FreeSqlPermissionStore` + SG 贡献者生成 |
| 迁移优先度 | 🟡 中（依赖 FreeSql 包，迁移需评估外部依赖） |

### Navigation（TKWF.Ext.Navigation）🔵 迁出候选

| 项 | 值 |
|----|----|
| 主框架版本 | V4.9.74（tag 已打）+ V4.9.76 NavigationOptions |
| 功能 | `MenuItemDefinition` + `IMenuContributor`（同步化）+ `MenuManager<TUserInfo>`（树形/循环检测/权限过滤）+ SG 菜单贡献者生成 |
| 迁移优先度 | 🟢 高（依赖 Permissions，待 Permissions 迁出后一并迁） |

---

## 四、规划中扩展（D17 Phase 4 基座业务扩展）

> 权威规划源：[D17-TKWF扩展机制与业务模块全景-设计方案.md](https://github.com/LoongBa/TKW.Framework/blob/master/docs/D17-TKWF扩展机制与业务模块全景-设计方案.md) §六 路线图 Phase 4（主框架仓库）。
> 本表为 D17 Phase 4 的执行跟踪镜像——D17 新增/调整扩展时同步本表。每项启动时：在下方登记 → 编写 `{扩展名}-开发方案.md` → Oracle/人工评审 → 实施 → 审核报告。

| 扩展 | 包名 | 状态 | 依赖 | 说明 |
|------|------|:----:|------|------|
| **Identity** | `TKWF.Ext.Identity` | ⚪ 候选 | — | 用户 + 角色 |
| **AuditLogging** | `TKWF.Ext.AuditLogging` | ⚪ 候选 | — | 审计日志 |
| **Settings** | `TKWF.Ext.Settings` | ⚪ 候选 | — | 设置管理 |
| **BlobStoring** | `TKWF.Ext.BlobStoring` | ⚪ 候选 | — | 文件存储 |
| **Emailing** | `TKWF.Ext.Emailing` | ⚪ 候选 | — | 邮件发送 |
| **Account** | `TKWF.Ext.Account` | ⚪ 候选 | Identity | 账户管理 |
| **Tagging** | `TKWF.Ext.Tagging` | ✅ 已迁出 | — | 已完成（D17 §六 Phase 4 已勾选 ✅，见上"已迁出扩展"） |

> **迁移/开发顺序建议**：Tagging（✅）→ Navigation（需 Permissions）→ Permissions（需评估 FreeSql 依赖）→ Identity → Account → 其余按需。

---

## 五、扩展 ADR 索引

> 扩展 ADR 存放 `docs/adr/`，命名 `ADR-{扩展名称}-{title}.md`（独立于主框架 ADR01-39 序号）。

| ADR | 标题 | 状态 | 关联扩展 |
|-----|------|:----:|---------|
| （暂无——Tagging 迁出沿用主框架决策，未产生新架构决策） | | | |

---

## 六、变更记录

| 日期 | 变更内容 | 关联 |
|------|---------|------|
| 2026-08-29 | 仓库初始化：Tagging 迁出（v4.9.79），Permissions/Navigation 登记为迁出候选，Phase 4 扩展登记候选 | — |